import { OcultarBotonesDirective } from './ocultar-botones.directive';

describe('OcultarBotonesDirective', () => {
  it('should create an instance', () => {
    const directive = new OcultarBotonesDirective();
    expect(directive).toBeTruthy();
  });
});
